#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

# Install common requirements first so any pip upgrade completes before the
# rest of the dependencies are installed.
python3 -m pip install --requirement requirements_common.txt
python3 -m pip install --requirement requirements_dev.txt